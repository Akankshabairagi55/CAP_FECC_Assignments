function compareNumbers(a, b) {
  if (a > b) {
    console.log(`${a} is greater than ${b}`);
  } else if (a == b) {
    console.log(`${a} is Equal to  ${b}`);
  } else {
    console.log(`${a} is less than ${b}`);
  }
}

compareNumbers(23, 56);
